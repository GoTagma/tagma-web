---
version: "0.7.5"
date: "2026-05-31"
channel: "alpha"
summary: |-
  - Add CI and npm publish hygiene gates for dependency integrity, focused tests, imports, cycles, secrets, formatting, publish metadata, and package selection validation
  - Secure embedded opencode server access with generated basic auth credentials and propagate auth headers through editor chat, health checks, provider auth removal, ensure, and restart flows
  - Improve Electron sidecar resilience by parsing readiness across stdout chunks, restarting crashed ready sidecars for open windows, reloading sessions onto the recovered backend, and updating navigation guards
  - Add workflow run event sequence numbers and replay-after support while sanitizing graph-end results from event payloads
  - Serialize editor pipeline local mutation requests so PATCH calls observe the latest revision and avoid stale revision conflicts
  - Harden run handling by ignoring stale lifecycle events while a new run id is still starting and marking target-excluded tasks with skipped results
  - Strengthen core and SDK behavior for safe-mode registry replacement/unregistration, skipped task results, and preservation of failure metadata in pipeline runner state
  - Harden hotupdate and opencode downloads with manifest JSON size limits, min shell semver validation, tarball redirect limits, and release manifest version validation
  - Improve LightRAG middleware by requiring configured API key environment variables and linking fetch cancellation to middleware abort signals
  - Add or update tests for modal accessibility, opencode client auth, sidecar recovery/stdout parsing, workflow gates, pipeline serialization, run targets, update manifests, and related editor/server workflows
---
