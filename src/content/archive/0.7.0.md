---
version: "0.7.0"
date: "2026-05-27"
channel: "alpha"
summary: |-
  - types: add SafeModeAllowlist interface for declarative safe-mode plugin policy
  - core: add DEFAULT_TASK_TIMEOUT_MS (30min) and defaultTaskTimeoutMs option on RunPipelineOptions
  - core: implement 3-way safe-mode allowlist merge (hardcoded + registry-declared + caller-supplied)
  - core: add RegisterPluginOptions.safeMode flag and PluginRegistry.getSafeModeDefaults()
  - core: extend RawDagEdge with kind field (explicit|dataflow) for visual edge distinction
  - core: buildRawDag infers dataflow edges from inputs/outputs name matching on direct dependencies only
  - core: add dataflow inference tests verifying edges are limited to direct dependency producers
  - sdk: split BuiltinTagmaPlugin into SafeBuiltinTagmaPlugin and UnsafeBuiltinTagmaPlugin
  - sdk: exclude output_check from safe-mode allowlist to prevent arbitrary shell execution bypass
  - sdk: re-export DEFAULT_TASK_TIMEOUT_MS and SafeModeAllowlist from @tagma/core
  - apps: split run.ts into run-session.ts (session classes/helpers) and run.ts (route handlers)
  - apps: remove legacy ws.runSession field, unify all session access to ws.runSessions Map
  - apps: add yamlVersion (FileVersion) to WorkspaceState for optimistic locking
  - apps: add optimistic-lock.ts with mtime-based conflict detection for YAML saves
  - apps: /api/save returns 409 Conflict when file has been modified externally since load
  - apps: document worker isolation architecture in plugins/worker-runtime.ts
  - apps: split chat-store.ts into chat-persist.ts, chat-editor-context.ts, chat-provider-catalog.ts
  - apps: implement diff-based undo/redo in pipeline-store using HistoryDiff instead of full snapshots
  - apps: fix undo/redo stack semantics — pop entries instead of replacing with completed diff
  - apps: fix TS2322 in undo/redo rollback paths with null guards on config/positions fields
  - apps: add DagEdge.kind to client types for dataflow edge rendering
  - apps: render dataflow edges as dashed lines with reduced opacity in BoardCanvas
  - apps: add WorkflowTimeline component showing Gantt-style execution bars per pipeline
  - apps: integrate WorkflowTimeline as collapsible section in WorkflowView sidebar
  - apps: add workflow integration tests covering abort, lifecycle retry, and cross-scenario combinations
  - apps: fix workflow integration test stop_when values to match SDK schema (success/failure/always)
  - apps: add retry cleanup in afterEach for Windows EBUSY temp directory removal
  - scripts: add incremental-build.mjs for git-aware partial package rebuilds
  - root: add build:incremental and build:incremental:force npm scripts
  - docs: add submodule consolidation evaluation document with decision framework
---
