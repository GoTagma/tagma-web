---
version: "0.10.6"
date: "2026-08-19"
channel: "alpha"
summary: |-
  - Preserve the tail of repair-prompt stdout/stderr evidence via head+tail clipping so the actionable error at the tail of a verbose serializer (PowerShell CLIXML) is no longer hidden
  - Share a single 8 KiB trial-stream bound (TRIAL_STREAM_EVIDENCE_BYTES) between the trial-run producer and the repair-prompt consumer to prevent re-truncation of an already-bounded stream
  - Remove the 500-char repair-prompt fallback cliff that truncated diagnostic streams
  - Accept 'unchanged' reconcile outcomes in result persistence so unchanged results are no longer silently dropped
  - Render the session pipeline result as a standalone bubble at the end of the conversation even when it is also fused into its owning message, keeping the Open pipeline action visible at the bottom
  - Ignore local-command transcript files at the repository root
  - Enable the Sandbox Trial run by default and stamp the current trial consent version for new workspaces while keeping unconsented existing workspaces at version 0
---
