---
version: "0.8.28"
date: "2026-06-22"
channel: "alpha"
summary: |-
  - Run page: changed failed, timeout, blocked, and non-zero-exit history task Ask AI actions from historical comparison to bug-fix mode, with status, exit code, command or prompt, stdout, stderr, normalized output, and full log paths attached as context.
  - Run page: made running workflow and pipeline loading indicators animate in history flow task cards and track panels instead of rendering as static icons.
  - Pipeline graph editor: changed graph nodes to prioritize the resolved pipeline name as the main label, show the graph instance ID as the subtitle, and keep the path visible as supporting metadata.
  - Pipeline graph editor: added per-pipeline Loop Count controls that default to 1 and persist values greater than 1 through workflow lifecycle max_runs with stop_when always, so each graph pipeline instance repeats an exact number of times.
  - Pipeline graph model: added loop count helpers and regression coverage for storing, updating, and removing fixed loop lifecycle settings.
  - Pipeline graph connections: covered the same-pipeline, different-instance case so repeated pipeline paths can be connected by distinct graph IDs without false circular dependency blocking.
  - Pipeline graph run UX: added a graph run result and runtime page showing running, succeeded, and failed states, graph run ID, per-pipeline runtime state, loop run counts, start and finish times, run IDs, errors, task events, abort, run again, and edit graph actions.
  - Tests: added HistoryFlowView coverage for Ask AI fix mode and spinning running icons, WorkflowView coverage for node naming, loop UI, and graph run result pages, plus workflow graph model coverage for repeated pipeline instances and loop persistence.
  - Verification: ran targeted editor tests, workflow and SDK related tests, TypeScript checks for app and test configs, Prettier checks, and git diff whitespace checks before committing.
---
